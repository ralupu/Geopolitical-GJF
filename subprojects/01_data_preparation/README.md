# SP01 — Data Preparation

**Status:** Pending  
**Script:** `prepare_data.py`  
**Outputs:** `../../data/panel_daily.parquet`, `../../data/shocks_long.csv`, `../../results/data_provenance/`

Copies the locked shock series and equity return panel from the parent repository into `data/`, builds the unified analysis panel, and writes a provenance summary.

See `ACTION_PLAN.md` Phase 1 for full specification.
