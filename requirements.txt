# Paper_GFJ — Python dependencies
# Install with: pip install -r requirements.txt

# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
statsmodels>=0.14       # VAR, OLS, Newey-West
scikit-learn>=1.3       # PCA (EMFI construction)

# Connectedness / network
networkx>=3.1           # Network graphs for connectedness figures

# HMM
hmmlearn>=0.3.0         # Gaussian HMM for stress regimes

# Data I/O
openpyxl>=3.1           # Read .xlsx files
pyarrow>=13.0           # Read/write .parquet
fastparquet>=2023.4     # Alternative parquet engine

# Visualization
matplotlib>=3.7
seaborn>=0.12

# Utilities
tqdm>=4.65              # Progress bars
joblib>=1.3             # Parallel execution

# Optional: TVP-VAR (for Phase 9 robustness)
# pyflux                # Uncomment if TVP-VAR robustness is implemented
